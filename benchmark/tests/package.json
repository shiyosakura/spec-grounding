{
  "name": "cancellation-policy-benchmark-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:spec": "BASE_URL=http://localhost:3097 vitest run",
    "test:vibe": "BASE_URL=http://localhost:3098 vitest run",
    "test:baseline": "BASE_URL=http://localhost:3099 vitest run"
  },
  "devDependencies": {
    "vitest": "^3.1.4"
  }
}
